代码清单描述：
1）KleeFloat 给出的是在添加到求解器后端的代码；
2）KQueryParser给出的是Parser的代码；
3）LFFSolver给出的是线性拟合串行求解器的代码；
4）PLFFSolver给出的是线性拟合并行求解器的代码；
5）IIP Solver是在LFF Solver基础上设计完成的，在LFFSolver和KleeFloat均有部分代码，所以没有单独的文件夹；

累计代码总量：3W+
